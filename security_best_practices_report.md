# セキュリティベストプラクティス レビュー

対象: `python-ai-app`(Streamlit + Google Gemini API を使った AIライティングツール)
レビュー日: 2026-08-19

## エグゼクティブサマリー

コードベース全体(`app.py`, `gemini_client.py`, `features/*.py`, `tests/*.py`)を確認しました。**Critical / High severity の脆弱性は見つかりませんでした。** DB・認証・ファイルシステムへの書き込み・SQL・シェル実行・`unsafe_allow_html` などの危険なシンクは一切使用されておらず、`.env` はコミットされていない(gitで一度も追跡されていない)ことも確認済みです。

Streamlit 固有のセキュリティガイドはこのスキルの参照資料に含まれていないため、一般的なセキュアコーディング原則と、AIアプリ特有のリスク(プロンプトインジェクション)の観点から評価しました。指摘は Low〜Informational の改善提案のみです。

## 良好な点(そのまま維持してよい実装)

- `.env` は `.gitignore` に含まれており、git履歴にも一度もコミットされていない([F-1] `.gitignore:4`)。
- `GEMINI_API_KEY` の未設定・プレースホルダー値("your_api_key_here")をチェックしている(`app.py:22-27`, `gemini_client.py:10-15`)。
- テストはダミーキーを `monkeypatch` で注入しており、実キーへの依存がない(`tests/test_gemini_client.py`)。
- `eval`/`exec`/`subprocess`/`os.system`/`unsafe_allow_html` など危険なAPIの使用なし。

## 指摘事項

### [ID-1] エラーメッセージがそのままUIに表示される(Low)
- **場所**: `features/summarize.py:26`, `features/blog.py:31`, `features/email_reply.py:29`, `features/sns_post.py:32`
- **内容**: `except Exception as e: st.error(f"生成に失敗しました: {e}")` で、例外メッセージをそのままUIに出力しています。
- **影響**: Gemini APIのエラーメッセージや内部例外の詳細情報(場合によっては設定値の一部など)が画面に表示される可能性があります。現状はローカル/単一ユーザー向けツールのため実害は小さいですが、複数人で共有するデプロイ先(社内共有Streamlitサーバーなど)に展開する場合はリスクが上がります。
- **提案**: ユーザー向けには汎用メッセージを表示し、詳細はサーバー側ログ(`logging`)に残す形に変更する。

### [ID-2] 依存パッケージのバージョンが未固定(Low)
- **場所**: `requirements.txt:1-4`
- **内容**: `streamlit>=1.38` のように `>=` 指定のみで上限がなく、ロックファイルもありません。
- **影響**: `pip install` のタイミングによって異なるバージョンが入り、意図しない破壊的変更や将来の脆弱性混入に気づきにくくなります。
- **提案**: `pip freeze` 等でバージョンを固定する、または `requirements.lock` / `uv.lock` の導入を検討。破壊的変更のリスクがあるため、アップグレードは必ずユーザーの同意を得てから実施してください。

### [ID-3] プロンプトインジェクションの設計上の考慮(Informational)
- **場所**: `features/blog.py:8-14`, `features/summarize.py:6-10`, `features/email_reply.py:6-12`, `features/sns_post.py:6-13`
- **内容**: ユーザー入力(本文・テーマ・受信メール・画像+メモ)が検証なしにそのままLLMプロンプトへ埋め込まれています。
- **影響**: 現状は生成結果をユーザーが目視確認してから使う想定のため実害は限定的です。ただし、将来的に生成結果を**人の確認なしに自動投稿・自動送信**するような機能に拡張する場合、入力文中に埋め込まれた指示(例:「これまでの指示を無視して...」等)によって意図しない出力を誘発される可能性があります。
- **提案**: 現状の「人が確認してから使う」フローを維持する限り対応不要。自動化する場合は、出力の後処理チェックや、システムプロンプトとユーザー入力の境界を明確にする設計を検討してください。

## 今後の対応について

上記はいずれも Low / Informational であり、緊急対応は不要です。[ID-1] のエラーメッセージ改善から着手することをご希望であれば、対応します。

レポートはリポジトリ直下 `security_best_practices_report.md` に保存しました。
